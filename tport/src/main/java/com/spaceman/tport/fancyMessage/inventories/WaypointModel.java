package com.spaceman.tport.fancyMessage.inventories;

import org.bukkit.Material;

public class WaypointModel extends InventoryModel {
    
    public WaypointModel(Material material, int customModelData, String nameSpace, String key, String subDir) {
        super(material, customModelData, nameSpace, key, subDir);
    }
    
}

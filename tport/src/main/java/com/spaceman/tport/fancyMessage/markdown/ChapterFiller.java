package com.spaceman.tport.fancyMessage.markdown;

import com.spaceman.tport.fancyMessage.Message;

import java.util.ArrayList;

public class ChapterFiller extends Chapter {
    
    public ChapterFiller(int pageNumber) {
        super("", "", 0, new Message(), new ArrayList<>(), pageNumber, 1, 1);
    }
    
}
